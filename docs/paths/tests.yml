get:
  security:
    - bearerAuth: []
  tags:
    - test1
  summary: test1 summary
  description: test1 description
  parameters:
    - $ref: "../parameters/test.yml"
  responses:
    200:
      content:
        application/json:
          schema:
            $ref: '../schemas/lesson.yml'
    401:
      $ref: "../responses/401.yml"
    403:
      $ref: "../responses/403.yml"
    422:
      $ref: "../responses/422.yml"
